import PrivateLayout from '@/components/PrivateLayout';

export default function CalificacionLayout({ children }: { children: React.ReactNode }) {
    return <PrivateLayout>{children}</PrivateLayout>;
}
